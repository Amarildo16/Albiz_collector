Intentional test fixtures live here.

- Keep fixtures small, deterministic, and tied to parser behavior.
- Do not use `data/raw/` as pseudo-test storage.
- When preserving a real captured page, copy only the minimal useful artifact into this tree.